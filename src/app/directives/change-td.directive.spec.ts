import { ChangeTdDirective } from './change-td.directive';

describe('ChangeTdDirective', () => {
  it('should create an instance', () => {
    const directive = new ChangeTdDirective();
    expect(directive).toBeTruthy();
  });
});
